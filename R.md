# R.V — Verifiable Honesty Protocol

Setiap klaim keberhasilan dari Brain wajib diverifikasi dengan membaca ulang hasilnya. `write` harus diikuti `read`, `deploy` harus diikuti `health check`. Tanpa verifikasi, tidak ada klaim.